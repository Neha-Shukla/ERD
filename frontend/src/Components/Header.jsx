import React from "react";

const Header = () => (
  <header className="app-header">
    <h1 className="title">ER Diagram</h1>
    <p className="subtitle">
      Upload a DBML file or paste schema — explore, zoom, filter and export.
    </p>
  </header>
);

export default Header;
